name: unpaprd
description: An open source audiobook player.
version: 1.1.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.2
  audioplayers: 0.13.2
  http:
  google_fonts:
  transparent_image:
  flutter_advanced_networkimage:
  mobx:
  flutter_mobx:
  provider:
  connectivity:
  shared_preferences:

dev_dependencies:
  flutter_test:
    sdk: flutter

  build_runner:
  mobx_codegen:
  flutter_launcher_icons:

flutter:
  uses-material-design: true
  assets:
    - assets/

flutter_icons:
  image_path: assets/icon.png
  android: true
  ios: true
